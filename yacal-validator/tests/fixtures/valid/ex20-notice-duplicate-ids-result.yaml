# VALID FIXTURE: NoticeExpression / Notice Id is NOT required to be unique. Duplicate
# notice Ids are permitted; this was an error fixture before an upstream spec change
# removed the uniqueness constraints.
Response:
  Result:
    - Decision: Permit
      Notice:
        - Id: "urn:example:yacal:notice:log"
        - Id: "urn:example:yacal:notice:log"
